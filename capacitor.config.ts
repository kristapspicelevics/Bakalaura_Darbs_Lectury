import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.app.lectury',
  appName: 'ionic-app-lectury',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
